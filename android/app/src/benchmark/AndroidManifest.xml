<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <!-- Agentic framework: ADB shell content call transport for benchmark builds -->
        <provider
            android:name="app.dqxn.android.agentic.AgenticContentProvider"
            android:authorities="${applicationId}.agentic"
            android:exported="true" />

    </application>

</manifest>
