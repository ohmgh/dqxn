package app.dqxn.android.data.layout

/** Grid column and row position on the unbounded canvas. */
public data class GridPosition(val col: Int, val row: Int)

/** Widget size in grid units. */
public data class GridSize(val widthUnits: Int, val heightUnits: Int)
