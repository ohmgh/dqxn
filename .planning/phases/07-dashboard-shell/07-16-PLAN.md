---
phase: 07-dashboard-shell
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "CLAUDE.md module dependency rules for :feature:dashboard include :core:agentic"
    - "Exception is documented with justification (SemanticsOwnerHolder for agentic debugging)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Documented :core:agentic dependency exception"
      contains: ":core:agentic"
  key_links:
    - from: "CLAUDE.md :feature:dashboard rules"
      to: ":core:agentic"
      via: "allowed dependency with documented justification"
      pattern: "core:agentic"
---

<objective>
Document the :core:agentic dependency exception for :feature:dashboard in CLAUDE.md.

Purpose: :feature:dashboard imports :core:agentic for SemanticsOwnerHolder registration in DashboardLayer.kt. This import is not listed in CLAUDE.md's module dependency rules. Moving SemanticsOwnerHolder to :sdk:contracts is not viable — it depends on compose.ui.semantics (SemanticsOwner, SemanticsNode, SemanticsProperties), SemanticsSnapshot, and SemanticsFilter, all of which are :core:agentic types and require the Compose compiler. :sdk:contracts has neither.

Output: CLAUDE.md updated with :core:agentic in :feature:dashboard's allowed dependencies, with a brief justification.
</objective>

<execution_context>
@/Users/ohm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ohm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@android/feature/dashboard/build.gradle.kts
@android/feature/dashboard/src/main/kotlin/app/dqxn/android/feature/dashboard/layer/DashboardLayer.kt
@android/core/agentic/src/main/kotlin/app/dqxn/android/core/agentic/SemanticsOwnerHolder.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add :core:agentic to :feature:dashboard allowed dependencies in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
In the "Module Dependency Rules" section, find the `:feature:dashboard` rule:

```
**`:feature:dashboard`** — CAN: `:sdk:*`, `:core:design`, `:core:thermal`, `:data`. CANNOT: any `:pack:*`.
```

Update to:

```
**`:feature:dashboard`** — CAN: `:sdk:*`, `:core:design`, `:core:thermal`, `:core:agentic` (debug semantics registration), `:data`. CANNOT: any `:pack:*`.
```

The parenthetical note explains the exception concisely. SemanticsOwnerHolder cannot move to :sdk:contracts because it depends on compose.ui.semantics types and requires the Compose compiler, neither of which :sdk:contracts has.

Do NOT make any other changes to CLAUDE.md.
  </action>
  <verify>
    <automated>grep -q "core:agentic" /Users/ohm/Workspace/dqxn/CLAUDE.md && echo "PASS: :core:agentic documented" || echo "FAIL: :core:agentic not found"</automated>
  </verify>
  <done>CLAUDE.md :feature:dashboard module rules include :core:agentic with justification.</done>
</task>

</tasks>

<verification>
```
grep ":feature:dashboard" CLAUDE.md
```
Verify the output shows `:core:agentic` in the allowed dependencies.
</verification>

<success_criteria>
- CLAUDE.md :feature:dashboard CAN list includes :core:agentic
- Justification documented inline (debug semantics registration)
- No other CLAUDE.md content changed
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-shell/07-16-SUMMARY.md`
</output>
